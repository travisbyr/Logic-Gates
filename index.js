// Create an XOR gate function
/*
function XORGate(a, b) {
  if (typeof a === "boolean" && typeof b === "boolean") {
    return (a && !b) || (!a && b);
  }
}

console.log(XORGate(true,false));

A = [0,0,0,0,1,1,1,1]
B = [0,0,1,1,0,0,1,1]
C = [0,1,0,1,0,1,0,1]

function truthTable(a,b,c) {
    if (typeof a === "boolean" && typeof b === "boolean" && typeof c === "boolean") {
        columns = function.length();
        return columns

    }
}

console.log(truthTable(A,B,C));

*/